// import supertest from 'supertest';
// import app from '../app.js';

describe('Panel Member assigned  Assigned presentations',() =>{
    describe('get panel member assigned presentations', () =>{
        describe('given the panel member assigned presentations',
            () => {
                it('should return a panel member assigned presentations', async () => {
                    expect(true).toBe(true);
                    // const presentaionID = '629832b8b9d2f2043f7891886';
                    // await supertest(app).get(`/makeSubmission/:id${presentaionID}`).expect(404);
                });
            });
    });
});

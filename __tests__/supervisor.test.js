// import supertest from 'supertest';
// import app from '../app.js';

describe('supervisor Assigned Teams',() =>{
    describe('get supervisor assigned teams', () =>{
        describe('given the supervisor exist teams',
            () => {
                it('should return a correct teams assigned to supervisor', async () => {
                    expect(true).toBe(true);
                    // const userId = '629832b8b9d2f2043f0f1886';
                    // await supertest(app).get(`api/researchGroup/supervisor/${userId}`).expect(404);
                });
            });
    });
});
